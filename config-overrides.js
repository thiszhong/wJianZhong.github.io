/* config-overrides.js */
// const path = require('path');

module.exports = function override(config, env) {
  //do stuff with the webpack config...
  // config.output.path = path.join(__dirname, 'blog');
  return config;
}